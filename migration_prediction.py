# -*- coding: utf-8 -*-
"""Migration prediction

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/10S5AwRxCMaE_bT-BBbScl2GrL3R6HySo
"""

import pandas as pd
import numpy as np
from sklearn.model_selection import train_test_split
from sklearn import svm
import seaborn as sns
import matplotlib.pyplot as plt
from sklearn.metrics import mean_squared_error
from sklearn.naive_bayes import GaussianNB

from google.colab import files

files = files.upload()

import io

df = pd.read_csv(io.BytesIO(files['migration_nz.csv']))

df.head()

df['Measure'].value_counts()

df['Measure'].replace("Arrivals", 0, inplace=True)
df['Measure'].replace("Departures", 1, inplace=True)
df['Measure'].replace("Net", 2, inplace=True)

df['Measure'].unique()

df['Country'].unique()

df['CountryID'] = pd.factorize(df['Country'])[0]
df['CitID'] = pd.factorize(df['Citizenship'])[0]

df['CountryID'].unique()

df.isnull().sum()

df["Value"]= df["Value"].fillna(df["Value"].median())

df.isnull().sum()

X = df[['CountryID','Measure','Year','CitID']]
y = df['Value']

X_train, X_test, y_train, y_test = train_test_split(X,y, test_size=0.3, random_state=9)

X_train.shape

from sklearn.ensemble import RandomForestRegressor
rf = RandomForestRegressor(n_estimators=70)
rf.fit(X_train,y_train)

rf.score(X_test,y_test)